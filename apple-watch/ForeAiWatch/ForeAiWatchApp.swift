import SwiftUI

// Kempton Park Golf — Apple Watch (watchOS) companion. Standalone GPS rangefinder
// (bundled course) plus the on-wrist lightning alarm + next tee times, mirroring
// the Android Wear OS app. watchOS 9+ single-target SwiftUI app (no extension).
@main
struct ForeAiWatchApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
