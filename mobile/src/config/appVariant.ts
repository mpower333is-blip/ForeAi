// ForeAi ships as a single app for iOS and Android.
//
// An earlier build system could emit a separate, cut-down "ECS Golf Day"
// variant from the same codebase. That's been removed — the golf-day
// experience now lives inside the one app: a player joins the event by code,
// which unlocks every feature for the day. One codebase, one app, one binary
// per platform.

export const APP_NAME = "ForeAi";
